# Problem 1: write a function to perform set intersect operator.
# i.e given 2 lists, find common elements in both lists.
# expect O(N+M) where N: size of list1, M: size of list2.
# algo : use collection/dict


def set_intersect(set1, set2):
    return None


# Problem 2: longest common prefix amongst strings
# Given a collection of strings, find the longest common
# prefix for these strings.
# Expect O(M*k) where M = length of longest common prefix, k= number of strings
# algo: sort strings by len, scan characters from left to right


def longest_common_prefix(strings):
    return None
